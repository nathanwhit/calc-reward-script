# reward-script

A script to calculate rewards for Creditcoin.

## Running

- Install deps

    ```bash
    yarn install
    ```

- Run

    ```bash
    yarn build-run
    ```

    or

    (uses `ts-node`)

    ```bash
    yarn run-script
    ```
